# Python Lambda Functions – 100 Questions

# Basic Level (1–30):

# Create a lambda function to add two numbers.

# Create a lambda function to subtract two numbers.

# Create a lambda function to multiply two numbers.

# Create a lambda function to divide two numbers.

# Create a lambda function to square a number.

# Create a lambda function to cube a number.

# Create a lambda function to find max of two numbers.

# Create a lambda function to find min of two numbers.

# Use lambda with map() to square elements of a list.

# Use lambda with map() to cube elements of a list.

# Use lambda with map() to double elements of a list.

# Use lambda with filter() to get even numbers from a list.

# Use lambda with filter() to get odd numbers from a list.

# Use lambda with filter() to get numbers greater than 5.

# Use lambda with reduce() to get sum of a list.

# Use lambda with reduce() to get product of a list.

# Use lambda with sorted() to sort list of tuples by first element.

# Use lambda with sorted() to sort list of tuples by second element.

# Use lambda with sorted() to sort list of strings by length.

# Create a lambda function to check if a number is even.

# Create a lambda function to check if a number is odd.

# Create a lambda function to check if number is positive.

# Create a lambda function to check if number is negative.

# Create a lambda function to find absolute value.

# Use lambda with map() to add 5 to each element of a list.

# Use lambda with map() to multiply each element by 3.

# Use lambda with filter() to get numbers divisible by 3.

# Use lambda with filter() to get numbers divisible by 5.

# Use lambda with map() and filter() together.

# Use lambda to convert list of strings to uppercase.

# Intermediate Level (31–70):
# 31. Use lambda to convert list of strings to lowercase.
# 32. Use lambda to capitalize first letter of each string.
# 33. Use lambda with sorted() to sort list of strings alphabetically.
# 34. Use lambda with sorted() to sort list of numbers descending.
# 35. Use lambda to find max in a list using reduce().
# 36. Use lambda to find min in a list using reduce().
# 37. Use lambda to calculate factorial using reduce().
# 38. Use lambda to sum squares of elements in a list.
# 39. Use lambda to sum cubes of elements in a list.
# 40. Use lambda to multiply all even numbers in a list.
# 41. Use lambda with map() to add two lists element-wise.
# 42. Use lambda with map() to subtract two lists element-wise.
# 43. Use lambda with map() to multiply two lists element-wise.
# 44. Use lambda with map() to divide two lists element-wise.
# 45. Use lambda to check if string is palindrome.
# 46. Use lambda to count vowels in a string.
# 47. Use lambda to count consonants in a string.
# 48. Use lambda with filter() to remove negative numbers.
# 49. Use lambda with filter() to keep numbers > 10.
# 50. Use lambda with filter() to keep numbers < 50.
# 51. Use lambda to flatten a list of lists using reduce().
# 52. Use lambda with map() to square only even numbers.
# 53. Use lambda with map() to cube only odd numbers.
# 54. Use lambda to find second largest element in a list.
# 55. Use lambda to find second smallest element in a list.
# 56. Use lambda to swap keys and values in a dictionary.
# 57. Use lambda with sorted() to sort dictionary by keys.
# 58. Use lambda with sorted() to sort dictionary by values.
# 59. Use lambda with filter() to get strings starting with vowel.
# 60. Use lambda with filter() to get strings ending with consonant.
# 61. Use lambda to check if number divisible by 2 and 3.
# 62. Use lambda to check if number divisible by 2 or 3.
# 63. Use lambda to get cumulative sum of a list.
# 64. Use lambda to get list of factorials using map().
# 65. Use lambda with filter() to get prime numbers in a list.
# 66. Use lambda to sum elements divisible by 3 in a list.
# 67. Use lambda to sum elements divisible by 5 in a list.
# 68. Use lambda to square numbers divisible by 2.
# 69. Use lambda to cube numbers divisible by 3.
# 70. Use lambda with map() and filter() to double even numbers only.

# Advanced Level (71–100):
# 71. Use lambda to sum squares of odd numbers only.
# 72. Use lambda to sum cubes of even numbers only.
# 73. Use lambda to create dictionary from list with squares as values.
# 74. Use lambda to create dictionary from list with cubes as values.
# 75. Use lambda to flatten list of tuples.
# 76. Use lambda to filter tuples with sum > 10.
# 77. Use lambda to filter tuples with sum < 5.
# 78. Use lambda to merge two dictionaries keeping max values.
# 79. Use lambda to merge two dictionaries keeping sum of values.
# 80. Use lambda to sort list of strings by last character.
# 81. Use lambda to sort list of strings by number of vowels.
# 82. Use lambda to count number of digits in list of numbers.
# 83. Use lambda to count number of even digits in a number.
# 84. Use lambda to count number of odd digits in a number.
# 85. Use lambda to check if number is Armstrong.
# 86. Use lambda to check if number is palindrome.
# 87. Use lambda to check if number is prime.
# 88. Use lambda with reduce() to find GCD of two numbers.
# 89. Use lambda with reduce() to find LCM of two numbers.
# 90. Use lambda to reverse each string in list.
# 91. Use lambda to capitalize first letter of each string in list.
# 92. Use lambda with map() to convert list of strings to their lengths.
# 93. Use lambda with filter() to remove strings of length < 5.
# 94. Use lambda to find maximum length string in a list.
# 95. Use lambda to find minimum length string in a list.
# 96. Use lambda with map() to convert list of numbers to string.
# 97. Use lambda to create acronym from list of words.
# 98. Use lambda with reduce() to concatenate list of strings.
# 99. Use lambda to square elements of nested list.
# 100. Use lambda to sum elements of nested list if element > 5.