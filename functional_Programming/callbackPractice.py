# Basic Level (1–30):

# Define a function that takes another function as argument and calls it.

# Define a function that adds two numbers using callback function.

# Define a function that multiplies two numbers using callback function.

# Define a function that prints "Hello" using callback function.

# Define a function that prints "World" using callback function.

# Create a callback function to check if a number is even.

# Create a callback function to check if a number is odd.

# Create a callback function to square a number.

# Create a callback function to cube a number.

# Create a callback function to print elements of a list.

# Create a function that sums elements of a list using callback.

# Create a function that multiplies elements of a list using callback.

# Create a function that filters even numbers using callback.

# Create a function that filters odd numbers using callback.

# Create a function that applies a callback to each element of a list.

# Create a function that finds maximum using callback.

# Create a function that finds minimum using callback.

# Create a callback to check if string length > 5.

# Create a callback to check if string starts with "P".

# Create a callback to check if string ends with "n".

# Use callback to print factorial of a number.

# Use callback to print Fibonacci series up to n.

# Create a function that sorts list using callback.

# Create a function that reverses a string using callback.

# Create a function that concatenates two strings using callback.

# Create a callback to print square of numbers in a list.

# Create a callback to print cube of numbers in a list.

# Use callback to find sum of digits of a number.

# Use callback to find product of digits of a number.

# Create a function that calls a callback after printing "Start" and before printing "End".

# Intermediate Level (31–70):
# 31. Use callback to check if all elements in list are positive.
# 32. Use callback to check if all elements in list are even.
# 33. Use callback to check if at least one element is negative.
# 34. Use callback to sum even numbers in a list.
# 35. Use callback to sum odd numbers in a list.
# 36. Use callback to find maximum even number in a list.
# 37. Use callback to find minimum odd number in a list.
# 38. Use callback to convert list of strings to integers.
# 39. Use callback to convert list of integers to strings.
# 40. Use callback to flatten nested list.
# 41. Use callback to filter numbers divisible by 3.
# 42. Use callback to filter numbers divisible by 5.
# 43. Use callback to create dictionary from two lists.
# 44. Use callback to find second largest element in a list.
# 45. Use callback to find second smallest element in a list.
# 46. Use callback to reverse each string in a list.
# 47. Use callback to capitalize each string in a list.
# 48. Use callback to remove duplicates from a list.
# 49. Use callback to sort list of strings by length.
# 50. Use callback to sort list of tuples by second element.
# 51. Create nested callbacks to perform multiple operations.
# 52. Use callback to calculate factorial of list of numbers.
# 53. Use callback to calculate square of list of numbers.
# 54. Use callback to calculate cube of list of numbers.
# 55. Use callback to check if numbers are prime in a list.
# 56. Use callback to sum squares of numbers in a list.
# 57. Use callback to sum cubes of numbers in a list.
# 58. Use callback to filter strings starting with vowel.
# 59. Use callback to filter strings ending with consonant.
# 60. Use callback to concatenate list of strings.
# 61. Use callback to sum elements of nested lists.
# 62. Use callback to multiply elements of nested lists.
# 63. Use callback to check palindrome numbers in a list.
# 64. Use callback to check Armstrong numbers in a list.
# 65. Use callback to check perfect numbers in a list.
# 66. Use callback to rotate list elements by n positions.
# 67. Use callback to merge multiple lists into one.
# 68. Use callback to filter unique elements from list.
# 69. Use callback to sum numbers divisible by both 2 and 3.
# 70. Use callback to sum numbers divisible by 3 or 5.

# Advanced Level (71–100):
# 71. Use callback to apply multiple transformations to list elements.
# 72. Use callback to apply function only to even numbers.
# 73. Use callback to apply function only to odd numbers.
# 74. Use callback to filter prime numbers and then square them.
# 75. Use callback to filter palindrome numbers and cube them.
# 76. Use callback to merge two dictionaries summing values.
# 77. Use callback to merge two dictionaries keeping max values.
# 78. Use callback to convert nested list of strings to integers.
# 79. Use callback to reverse strings and then capitalize first letter.
# 80. Use callback to sort list of dictionaries by key value.
# 81. Use callback to filter strings containing vowels.
# 82. Use callback to filter strings of length > 5.
# 83. Use callback to calculate factorial of list of numbers using reduce.
# 84. Use callback to calculate product of list of numbers using reduce.
# 85. Use callback to flatten nested tuples.
# 86. Use callback to create acronym from list of words.
# 87. Use callback to convert numbers to binary and sum them.
# 88. Use callback to convert numbers to hexadecimal and sum them.
# 89. Use callback to sum ASCII values of characters in string.
# 90. Use callback to check if string contains only digits.
# 91. Use callback to check if string contains only alphabets.
# 92. Use callback to sum digits of all numbers in a list.
# 93. Use callback to count vowels in list of strings.
# 94. Use callback to count consonants in list of strings.
# 95. Use callback to apply function on nested dictionary values.
# 96. Use callback to filter positive numbers and multiply them.
# 97. Use callback to filter negative numbers and sum them.
# 98. Use callback to check if nested list elements are all even.
# 99. Use callback to check if nested list elements are all odd.
# 100. Use callback to perform sequence of operations: filter → map → reduce.