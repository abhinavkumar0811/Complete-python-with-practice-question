# Basic Level (1–30):

# Create a simple decorator that prints "Before function" before calling a function.

# Create a decorator that prints "After function" after calling a function.

# Create a decorator that prints "Before" and "After" around a function call.

# Create a decorator that prints the name of the function being called.

# Create a decorator that prints the arguments passed to the function.

# Create a decorator that prints the return value of the function.

# Create a decorator that multiplies the return value by 2.

# Create a decorator that adds 5 to the return value.

# Create a decorator that converts return value to string.

# Create a decorator that converts return value to float.

# Create a decorator that checks if first argument is positive.

# Create a decorator that checks if first argument is negative.

# Create a decorator that prints "Function executed" after execution.

# Create a decorator that logs function call to console.

# Create a decorator that counts how many times a function is called.

# Create a decorator that prints "Start" before and "End" after function.

# Create a decorator that adds two numbers passed to the function.

# Create a decorator that multiplies two numbers passed to the function.

# Create a decorator that reverses string returned by function.

# Create a decorator that capitalizes string returned by function.

# Create a decorator that prints "Executing function" before function.

# Create a decorator that prints "Execution complete" after function.

# Create a decorator that converts all arguments to integer.

# Create a decorator that converts all arguments to float.

# Create a decorator that converts all arguments to string.

# Create a decorator that logs timestamp before function call.

# Create a decorator that logs timestamp after function call.

# Create a decorator that prints function name and arguments.

# Create a decorator that prints function name, arguments, and return value.

# Create a decorator that returns double the original return value.

# Intermediate Level (31–70):
# 31. Create a decorator that prints "Start" → function call → "End".
# 32. Create a decorator that accepts arguments itself.
# 33. Create a decorator that adds n to return value, n passed to decorator.
# 34. Create a decorator that multiplies return value by n, n passed to decorator.
# 35. Create a decorator that repeats the function call 2 times.
# 36. Create a decorator that repeats the function call n times.
# 37. Create a decorator that measures execution time of function.
# 38. Create a decorator that logs execution time and function name.
# 39. Create a decorator that validates if argument is positive.
# 40. Create a decorator that validates if argument is negative.
# 41. Create a decorator that converts all string arguments to uppercase.
# 42. Create a decorator that converts all string arguments to lowercase.
# 43. Create a decorator that ensures function returns integer.
# 44. Create a decorator that ensures function returns float.
# 45. Create a decorator that ensures function returns string.
# 46. Create a decorator that logs function call in a file.
# 47. Create a decorator that logs function arguments in a file.
# 48. Create a decorator that logs function return value in a file.
# 49. Create a decorator that prints a message only the first time function is called.
# 50. Create a decorator that prints a message only the last time function is called.
# 51. Create a decorator that caches results of function (simple memoization).
# 52. Create a decorator that counts and prints number of calls.
# 53. Create a decorator that converts return value to boolean.
# 54. Create a decorator that prints "Even" or "Odd" based on return value.
# 55. Create a decorator that adds pre-processing logic before function call.
# 56. Create a decorator that adds post-processing logic after function call.
# 57. Create a decorator that modifies string returned by function to remove spaces.
# 58. Create a decorator that modifies string returned by function to add prefix.
# 59. Create a decorator that modifies string returned by function to add suffix.
# 60. Create a decorator that checks if return value is None.
# 61. Create a decorator that prints "None returned" if function returns None.
# 62. Create a decorator that prints "Function executed successfully" on completion.
# 63. Create a decorator that handles exceptions inside function.
# 64. Create a decorator that retries function call if exception occurs.
# 65. Create a decorator that logs exceptions to a file.
# 66. Create a decorator that converts numeric return values to percentage.
# 67. Create a decorator that formats float return value to 2 decimals.
# 68. Create a decorator that logs start and end time in readable format.
# 69. Create a decorator that prints args and kwargs separately.
# 70. Create a decorator that prints length of string returned by function.

# Advanced Level (71–100):
# 71. Create a decorator that measures memory used by function.
# 72. Create a decorator that prints the type of return value.
# 73. Create a decorator that modifies function arguments before passing.
# 74. Create a decorator that swaps first two arguments before function call.
# 75. Create a decorator that adds logging for both input and output.
# 76. Create a decorator that times recursive function calls.
# 77. Create a decorator that counts nested recursive calls.
# 78. Create a decorator that converts tuple arguments to list before function.
# 79. Create a decorator that converts list arguments to tuple before function.
# 80. Create a decorator that converts all number arguments to float.
# 81. Create a decorator that converts all number arguments to integer.
# 82. Create a decorator that prints prime numbers from function return (if list).
# 83. Create a decorator that prints even numbers from function return (if list).
# 84. Create a decorator that prints odd numbers from function return (if list).
# 85. Create a decorator that logs function execution to CSV file.
# 86. Create a decorator that logs function arguments and results to JSON file.
# 87. Create a decorator that validates number of arguments passed.
# 88. Create a decorator that modifies returned dictionary values.
# 89. Create a decorator that adds timestamp key to returned dictionary.
# 90. Create a decorator that wraps multiple decorators on a single function.
# 91. Create a decorator that applies another decorator conditionally.
# 92. Create a decorator that delays function execution by n seconds.
# 93. Create a decorator that runs function asynchronously using threading.
# 94. Create a decorator that prints Fibonacci sequence using callback inside.
# 95. Create a decorator that memoizes recursive Fibonacci function.
# 96. Create a decorator that counts characters in returned string.
# 97. Create a decorator that reverses order of list returned by function.
# 98. Create a decorator that converts nested list to flat list.
# 99. Create a decorator that logs execution details to console and file simultaneously.
# 100. Create a decorator that dynamically modifies function behavior based on arguments.