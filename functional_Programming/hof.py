# Python HOF Functions – 100 Questions

# Map Function (1–30):

# Use map to double all numbers in a list.

# Use map to square all numbers in a list.

# Use map to convert list of strings to uppercase.

# Use map to convert list of strings to lowercase.

# Use map to convert list of integers to strings.

# Use map to calculate cube of each number.

# Use map with a lambda to add 5 to each element.

# Use map with a lambda to subtract 3 from each element.

# Use map to calculate factorial of each number in a list.

# Use map to find length of each string in a list.

# Use map with two lists to sum elements pairwise.

# Use map with two lists to multiply elements pairwise.

# Use map to convert list of floats to integers.

# Use map to convert list of integers to floats.

# Use map with function to reverse each string in a list.

# Use map with lambda to find square of even numbers only.

# Use map with lambda to find cube of odd numbers only.

# Use map with function to capitalize each string.

# Use map with lambda to replace vowels with *.

# Use map to calculate cumulative sum of list.

# Use map to add corresponding elements of three lists.

# Use map to create tuples (element, element^2).

# Use map to create tuples (element, index).

# Use map with lambda to multiply by 2 only if number is even.

# Use map with lambda to subtract 1 only if number is odd.

# Use map to convert list of boolean strings "True"/"False" to boolean.

# Use map to strip spaces from list of strings.

# Use map with function to reverse each word in a list.

# Use map to apply len() on each string in a nested list.

# Use map with lambda to return "Even"/"Odd" for list of numbers.

# Filter Function (31–60):
# 31. Use filter to select even numbers.
# 32. Use filter to select odd numbers.
# 33. Use filter to select positive numbers.
# 34. Use filter to select negative numbers.
# 35. Use filter to select numbers divisible by 3.
# 36. Use filter to select numbers divisible by 5.
# 37. Use filter to select numbers divisible by both 2 and 3.
# 38. Use filter to select strings starting with "A".
# 39. Use filter to select strings ending with "a".
# 40. Use filter to select non-empty strings.
# 41. Use filter to remove vowels from string list.
# 42. Use filter to select prime numbers from list.
# 43. Use filter to select palindromes from string list.
# 44. Use filter to remove zeros from list.
# 45. Use filter to select numbers > 10.
# 46. Use filter to select numbers < 5.
# 47. Use filter with lambda to select numbers divisible by 4.
# 48. Use filter with lambda to remove negative numbers.
# 49. Use filter with lambda to select strings of length > 3.
# 50. Use filter with lambda to select numbers which are perfect squares.
# 51. Use filter with lambda to select uppercase strings.
# 52. Use filter with lambda to select lowercase strings.
# 53. Use filter to select numbers divisible by 2 or 3.
# 54. Use filter to select numbers not divisible by 2.
# 55. Use filter to remove duplicate numbers from list.
# 56. Use filter to remove non-alphabetic strings.
# 57. Use filter to select numbers in a specific range.
# 58. Use filter to select elements which are instances of int.
# 59. Use filter to select elements which are instances of str.
# 60. Use filter to select non-empty nested lists.

# Reduce Function (61–85):
# 61. Use reduce to sum all elements.
# 62. Use reduce to multiply all elements.
# 63. Use reduce to find maximum element.
# 64. Use reduce to find minimum element.
# 65. Use reduce to find product of even numbers only.
# 66. Use reduce to sum odd numbers only.
# 67. Use reduce to concatenate list of strings.
# 68. Use reduce to find cumulative sum list.
# 69. Use reduce to find cumulative product list.
# 70. Use reduce to find GCD of list of numbers.
# 71. Use reduce to find LCM of list of numbers.
# 72. Use reduce to find string with maximum length.
# 73. Use reduce to merge list of dictionaries.
# 74. Use reduce to find max sum pair from two lists.
# 75. Use reduce to flatten a nested list.
# 76. Use reduce to combine multiple strings with -.
# 77. Use reduce with lambda to subtract elements in sequence.
# 78. Use reduce with lambda to divide elements sequentially.
# 79. Use reduce to find element appearing maximum times.
# 80. Use reduce to count total elements in nested list.
# 81. Use reduce to create cumulative XOR of list.
# 82. Use reduce to find first non-zero cumulative sum.
# 83. Use reduce to merge multiple sets.
# 84. Use reduce to find largest string alphabetically.
# 85. Use reduce to multiply squares of numbers.

# Reversed Function & Mixed HOF (86–100):
# 86. Use reversed to reverse a list.
# 87. Use reversed to reverse a string.
# 88. Use reversed with list comprehension to create reversed list.
# 89. Use reversed to iterate a tuple backwards.
# 90. Use reversed to iterate a range backwards.
# 91. Combine map and reversed to double numbers in reversed list.
# 92. Combine filter and reversed to get even numbers reversed.
# 93. Combine map and filter to square even numbers.
# 94. Combine map, filter, reduce to sum squares of even numbers.
# 95. Combine map, filter, reversed to process string list.
# 96. Use reduce and reversed to concatenate strings in reverse order.
# 97. Use map, filter to convert and filter list of strings.
# 98. Use filter and reduce to sum numbers divisible by 3.
# 99. Use map, filter, reduce, reversed together in a program.
# 100. Create HOF program: filter odd, double, reverse, then sum.