# Basic Level (1–30):

# Create a tuple t = (1, 2, 3, 4, 5).

# Print the tuple t.

# Print the first element of tuple t.

# Print the last element of tuple t.

# Print the length of tuple t.

# Create a tuple with a single element 10.

# Create a tuple t1 = 1, 2, 3 without parentheses.

# Access the 3rd element using negative indexing.

# Slice first 3 elements of t.

# Slice last 2 elements of t.

# Check if 3 is in t.

# Check if 6 is not in t.

# Concatenate two tuples (1, 2) and (3, 4).

# Repeat tuple (1, 2) three times.

# Find maximum element of tuple (5, 2, 8, 1).

# Find minimum element of tuple (5, 2, 8, 1).

# Count occurrences of 2 in (1, 2, 3, 2, 2).

# Find index of 3 in (1, 2, 3, 4).

# Convert a list [1, 2, 3] to tuple.

# Convert a string "Python" to tuple.

# Unpack tuple (1, 2, 3) into variables a, b, c.

# Swap two tuples t1 = (1, 2) and t2 = (3, 4).

# Print tuple elements using a for loop.

# Print tuple elements using while loop.

# Check if tuple is empty.

# Create a tuple with mixed data types (1, "Python", 3.5).

# Create nested tuple (1, 2, (3, 4)).

# Access element 4 from nested tuple.

# Check if tuple is immutable by trying to change first element.

# Delete an entire tuple using del.

# Intermediate Level (31–70):
# 31. Convert tuple (1, 2, 3) to a list.
# 32. Reverse a tuple (1, 2, 3, 4).
# 33. Sort tuple (3, 1, 4, 2) using list conversion.
# 34. Merge two tuples (1, 2) and (3, 4, 5).
# 35. Find sum of numeric elements in (1, 2, 3, 4).
# 36. Check length of nested tuple (1, (2, 3), 4).
# 37. Flatten nested tuple (1, (2, 3), 4).
# 38. Count elements of tuple (1, 2, 3, 2, 2) equal to 2.
# 39. Extract even numbers from tuple (1, 2, 3, 4, 5).
# 40. Extract odd numbers from tuple (1, 2, 3, 4, 5).
# 41. Find product of all elements in tuple (1, 2, 3, 4).
# 42. Find second largest element in (3, 1, 4, 2).
# 43. Find second smallest element in (3, 1, 4, 2).
# 44. Check if tuple (1, 2, 3) equals (1, 2, 3).
# 45. Check if tuple (1, 2) is a subset of (1, 2, 3).
# 46. Create tuple with repeated elements (5,) * 5.
# 47. Convert tuple of numbers to a tuple of strings.
# 48. Convert tuple of strings to a tuple of integers.
# 49. Use slicing to get every second element of (1, 2, 3, 4, 5).
# 50. Use slicing to reverse tuple (1, 2, 3, 4, 5).
# 51. Extract middle element of tuple (1, 2, 3, 4, 5).
# 52. Merge multiple tuples (1, 2), (3, 4), (5, 6).
# 53. Count total occurrences of element 2 in (1, 2, 3, 2, 2, 4).
# 54. Find index of last occurrence of 2.
# 55. Create tuple from dictionary keys.
# 56. Create tuple from dictionary values.
# 57. Iterate through tuple using enumerate().
# 58. Check if all elements are numbers using all().
# 59. Check if any element is negative using any().
# 60. Convert tuple (1, 2, 3) to set.
# 61. Create a tuple from range of numbers.
# 62. Create tuple with squares of numbers 1–5.
# 63. Create tuple with cubes of numbers 1–5.
# 64. Check if tuple is hashable.
# 65. Concatenate multiple tuples dynamically using a loop.
# 66. Extract elements at even indices from tuple.
# 67. Extract elements at odd indices from tuple.
# 68. Count elements greater than 3 in (1, 2, 3, 4, 5).
# 69. Multiply all numeric elements in tuple.
# 70. Find average of numeric elements in tuple (2, 4, 6, 8).

# Advanced Level (71–100):
# 71. Extract all strings from tuple (1, "a", 3.5, "b").
# 72. Extract all integers from tuple (1, "a", 3, "b").
# 73. Extract all floats from tuple (1, 2.5, 3.5, 4).
# 74. Count number of data types in tuple (1, "a", 2.5, True).
# 75. Flatten tuple of tuples ((1, 2), (3, 4)).
# 76. Convert tuple (1, 2, 3) to dictionary with indices as keys.
# 77. Convert tuple ("a", "b", "c") to dictionary with values as keys and index as values.
# 78. Find symmetric difference between two tuples converted to sets.
# 79. Merge two tuples and remove duplicates.
# 80. Create tuple of tuples (1, 2), (3, 4), (5, 6) and access element 6.
# 81. Create a tuple with only unique elements from (1, 2, 2, 3, 3, 4).
# 82. Count all vowels in tuple ("apple", "orange", "banana").
# 83. Count all consonants in tuple ("apple", "orange", "banana").
# 84. Reverse each element in tuple ("apple", "banana").
# 85. Find tuple with maximum length string.
# 86. Find tuple with minimum length string.
# 87. Create tuple of first letters of each string in ("apple", "banana", "cherry").
# 88. Create tuple of lengths of strings in ("apple", "banana", "cherry").
# 89. Check if tuple is subset of another tuple.
# 90. Create tuple of tuples from two lists using zip().
# 91. Create tuple of cumulative sums (1,2,3,4) → (1,3,6,10).
# 92. Create tuple with squares of only even numbers.
# 93. Create tuple with cubes of only odd numbers.
# 94. Check if tuple (1,2,3,1,2) contains duplicates.
# 95. Remove duplicates while keeping order in tuple (1,2,3,1,2).
# 96. Create tuple from list of strings by reversing each string.
# 97. Extract first and last elements of each nested tuple ((1,2),(3,4)).
# 98. Convert list of tuples [(1,2),(3,4)] to a flat tuple (1,2,3,4).
# 99. Create tuple with alternating elements from two tuples.
# 100. Create a tuple of tuples containing element and its square (1,1),(2,4),….