# List Comprehension (1–40):

# Create a list of numbers from 1 to 10 using list comprehension.

# Create a list of squares from 1 to 10.

# Create a list of cubes from 1 to 10.

# Create a list of even numbers from 1 to 20.

# Create a list of odd numbers from 1 to 20.

# Create a list of numbers divisible by 3 from 1 to 30.

# Create a list of numbers divisible by 5 from 1 to 50.

# Convert a list of strings to uppercase.

# Convert a list of strings to lowercase.

# Get lengths of all strings in a list.

# Filter positive numbers from a list.

# Filter negative numbers from a list.

# Multiply each element of a list by 2.

# Square each even number in a list.

# Cube each odd number in a list.

# Add 5 to each element in a list.

# Subtract 3 from each element in a list.

# Extract first letter of each string in a list.

# Extract last letter of each string in a list.

# Reverse each string in a list.

# Filter strings starting with "A".

# Filter strings ending with "a".

# Filter strings containing "Python".

# Replace vowels with * in each string.

# Remove spaces from each string.

# Create list of numbers from two lists added pairwise.

# Create list of numbers from two lists multiplied pairwise.

# Create list of tuples (element, element^2).

# Create list of tuples (element, index).

# Flatten a nested list.

# Flatten a nested list and square elements.

# Filter prime numbers from a list.

# Find numbers divisible by both 2 and 3.

# Find numbers divisible by 2 or 3.

# Filter numbers not divisible by 2.

# Convert list of strings to list of ASCII sums.

# Create list of factorials of numbers 1 to 5.

# Get cumulative sum list [1,3,6,10,…].

# Create a list of even squares.

# Create a list of odd cubes.

# Set Comprehension (41–60):
# 41. Create a set of numbers from 1 to 10.
# 42. Create a set of squares from 1 to 10.
# 43. Create a set of cubes from 1 to 10.
# 44. Create a set of even numbers from 1 to 20.
# 45. Create a set of odd numbers from 1 to 20.
# 46. Filter positive numbers into set.
# 47. Filter negative numbers into set.
# 48. Convert list of strings to uppercase set.
# 49. Extract first letters of strings into set.
# 50. Flatten nested list into set.
# 51. Set of unique characters from a string.
# 52. Set of vowels from string.
# 53. Set of consonants from string.
# 54. Set of numbers divisible by 3.
# 55. Set of numbers divisible by 5.
# 56. Set of numbers divisible by both 2 and 3.
# 57. Set of elements squared.
# 58. Set of elements cubed.
# 59. Set of prime numbers from list.
# 60. Set of unique words from list of strings.

# Dictionary Comprehension (61–85):
# 61. Create dict {x:x^2} for numbers 1–5.
# 62. Create dict {x:x^3} for numbers 1–5.
# 63. Create dict {x:x+5} for numbers 1–5.
# 64. Create dict {x:x-2} for numbers 1–5.
# 65. Create dict from two lists {key:value}.
# 66. Swap keys and values of a dict using comprehension.
# 67. Filter dict values > 10.
# 68. Filter dict keys starting with "A".
# 69. Square values of dict.
# 70. Convert dict values to string.
# 71. Convert dict keys to uppercase.
# 72. Reverse string values of dict.
# 73. Increment all numeric values by 1.
# 74. Decrement all numeric values by 2.
# 75. Filter dict items where value is even.
# 76. Filter dict items where value is odd.
# 77. Create dict of element:index from list.
# 78. Create dict of element:element^3 from list.
# 79. Merge two dicts with comprehension.
# 80. Create dict of string:length for list of strings.
# 81. Create dict of string:first_char for list of strings.
# 82. Create dict of string:last_char for list of strings.
# 83. Filter dict items with key length > 3.
# 84. Filter dict items with value > 10.
# 85. Update dict values by multiplying by 2.

# Nested & Conditional Comprehensions (86–100):
# 86. Create nested list [[i+j for j in range(3)] for i in range(3)].
# 87. Flatten nested list using comprehension.
# 88. Nested dict comprehension {i:{j:i*j for j in range(3)} for i in range(3)}.
# 89. Filter even numbers from nested list.
# 90. Filter odd numbers from nested list.
# 91. Create list of tuples (i,j) for i in 1–3, j in 1–3.
# 92. Conditional list comprehension: numbers divisible by 2 else 0.
# 93. Conditional dict comprehension: value squared if even else value.
# 94. Nested comprehension with strings: [c.upper() for word in words for c in word].
# 95. Create list of palindromes from list of strings.
# 96. Flatten nested list and filter even numbers.
# 97. Create dict of string:number of vowels.
# 98. Create set of lengths of words in list.
# 99. Nested set comprehension from list of lists.
# 100. Combine map/filter in comprehension: double even numbers from list.